[project]
name = "terra-sr"
version = "1.0.0"
description = "Deep Learning Based Super Resolution Mapping from Medium Resolution Satellite Imagery"
readme = "README.md"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "api.index:handler"
